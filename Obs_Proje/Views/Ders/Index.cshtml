@*@model IEnumerable<Obs_Proje.Data.Ders>*@
@model IEnumerable<DersViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Ders Listesi</h1>

<p>
    <a asp-action="Create">Yeni Ders Kaydet</a>
</p>
<table class="table">
    <thead>
        <tr>
            <h5>Toplam Ders Sayısı: @ViewBag.DersSayisi</h5>
            <th>
                @Html.DisplayNameFor(model => model.Adi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BolumAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OgretmenAdiSoyadi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BolumAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OgretmenAdiSoyadi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
